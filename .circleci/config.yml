version: 2.1
jobs:
  build:
    docker:
      - image: circleci/python:3.9
    steps:
      - checkout:
          keys:
            - 'C:/Users/jayan/Downloads/hw1/.ssh/project_key'

      - run:
          name: Installing dependencies
          command: |
            pip install -r requirements.txt
            python -m spacy download en_core_web_sm

      
      - run:
          name: Run tests
          command: |
            pytest test_api.py

workflows:
  version: 1
  build-and-deploy:
    jobs:
      - build
